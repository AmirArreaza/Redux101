pipeline {

    environment {
        PROJECT_NAME = "Redux - 101"
    }

    stages {
        stage('output') {
            steps {
                sh "echo ${env.PROJECT_NAME}"
            }
        }
    }
}